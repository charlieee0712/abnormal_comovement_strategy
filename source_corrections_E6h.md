# E6h 源码 / 前轮结论更正

本文只收**读源码或实测之后发现与 brief / plan / 前轮结论不符**的条目。
每条写：声称 → 实际 → 影响。

---

## §1 `build_core_g` 对两段式核返回的 `cand` 是**第一关幸存者**，不是全池

**声称**（我最初的实现假设）：`cand` 是候选域，`D = cand ∩ 有限分数 \ C` 就是经济拒绝集。

**实际**（读 `e6g_desc.GCtx.build_core_g` 末行 `return m, score, s1`）：两段式分支返回的
第三个值是 `s1` = **第一关幸存者**。所以对 R1（`KT_dep(50,50)`）而言，按上式算出的 D
只含**第二关**拒绝，第一关拒绝的 92,577 格整块漏掉。

**影响**：plan §4.1 明确要求 DEP 的**第一关例外 / 第二关例外 / 双关例外各自成卡**，
漏掉第一关拒绝集就无法建"第一关例外"那张卡。已补 `dep_stages()`，
补后 `S1 93,160 + 第一关拒绝 92,577 = 185,737 = pool0` 恰好闭合。

## §2 `e6g_expand` 里的 `D` 是 `e6f_desc`，不是 `e6g_desc`

**声称**：照抄 `e6g_expand.h4b` 的构造代码时按名字推断 `D = e6g_desc`。

**实际**：`e6g_expand.py` 第 16 行 `import e6f_desc as D`。`e6g_desc` 没有 `DC` / `DCF` /
`veto_hard`。

**影响**：T4 四段一起就 `AttributeError`，无错误数字外流。改后 560 个 ID 全单射，
且与 E6g 重叠的 192 个 ID **逐字对上**、数值**逐位相符**（max\|d_net8\| ~1e-15）。

## §3 `e6g_core.assert_registered_spec` 只放行 `U35 ∪ NEW40`

**实际**：该函数对 `role='F'` 的键做 `if k not in _NEW40_SET and k not in U35: raise`。

**影响**：本轮要用的 TCV / JUMP / IND_* 三类新派生键**无法**走 E6g 的注册闸，
而 §0.2 硬约束③ 规定 `e6g_*.py` 一个字都不能改。所以 `e6h_core.py` 建了自己的
注册层**包**而不**改** E6g：`assert_registered_h` 放行 87 个键（U74 75 + 派生 12），
已有键仍委托回 `G.assert_registered_spec`。`get_pct_h` 对已有键与 `get_pct_g`
**逐位相同**（5 键抽验全 True）。

## §4 `ρ = 1.000` 不等于别名；NS 是**线性**回归，单调非线性变换后排序会变

**声称**（我自己发出过的一条判断，已更正）：`gap_vs_sector` / `gap_rank_in_sector` /
`overnight_ret_cross_sectional_rank` 与 `overnight_ret` "是同一个截面序"，
机理是"减逐日行常数不改截面序"。

**实际**：逐格相同率 93.1% / 27.7% / 27.7%，**逐日排序完全一致只有 157/1193、0/1193、
0/1193 日**。错因两处：(a) 边表里 `rho_mean` 四舍五入到三位，0.9996 显示成 1.000；
(b) 用于选择的 pct 取自**中性化之后**，而 NS 是**线性**回归 —— `gap = (open−lclose)/lclose`
与 `overnight_ret = log(open/lclose)` 虽互为单调变换，但**线性**中性化后残差排序不同。

更强的反例：`gap_rank_in_sector` vs `overnight_ret_cross_sectional_rank` 的
`rho = 1.000000`（**六位小数**）而同格率只有 99.7%、max\|d\| 0.018。

**影响**：plan §5.2 的"精确别名仅一票"只能用在**真别名**上。实测 60 对高相关里
只有 **2 条真别名**（`CVR ≡ intraday_cvr_1d`，两段各一，max\|d\| = 0.00e+00 且 NaN 位一致）。
另记：`OWN_RET_5`（复权）vs `cum_return_5d`（未复权）只有 636/1193 日排序一致 ——
新造的复权端点收益**不是**未复权版的冗余。证据在 `registry/alias_measurements.csv`。

## §5 E6g 的"无冲击最优 H = 网格下界 3，四段一致"在扩网格后不成立

**声称**（E6g REPORT part2 / REVIEW）：无冲击最优 H 是网格下界 3，四段一致。

**实际**：把网格往下扩到 H=1 之后，逐格最优的分布是 **H=1:12 / H=2:22 / H=3:20 /
H=5:10**（64 格 = 4 段 × 8 账户 × 2 预算）。中位曲线峰在 H=2–3，H₂>H₁ 占 78%。

**影响**："四段一致等于 3"被推翻；但**下界仍在 12/64 格上绑定**，所以这只是
**部分**解开，不能写成"边界问题已解决"。生产 H=5 相对各格最优的损失：
中位 0.98 点、p95 2.16 点（无冲击口径）。

另记：含冲击（sqrt, κ=0.5）最优 H 随 A 外移，A=5 亿时四段全内点（15–20，证实 E6g），
但 **A≥10 亿时 2010-14 与 2019-23 顶到新上界 30** —— 大资金端边界重新出现。

## §6 E6g 的"缓冲单调升到 b=15（网格上界）"在扩到 b=30 后只部分成立

**实际**：逐格最优 b 的分布是 5:57 / 10:39 / 15:41 / 20:41 / **30:78**（256 格）。
**70% 的格峰在内点（5–20），但仍有 30% 顶在新上界 30。**
边际（相对 b=0，中位）：b5 +0.086、b10 +0.135、b15 +0.150、b20 +0.153、b30 +0.175。

## §7 brief 说 `E6g_REVIEW.md` 指纹"与 plan 一致"，实际不符

**实际**：plan 附录 C 记 `ef36aa7f…`，本地实际 `a517b7b7…`。原因与 proposal 那处
**同源同因**：规划 session 在 plan 产出（22:55）之后、于 23:14 把负号勘误
（复盘 #33）**同时**补进了 proposal 顶部与 `E6g_REVIEW` §Q1。

**影响**：不阻断。实际值与 plan 值并列进 `source_manifest.json`，按 brief 记 WARN。

## §8 M-T 的增益是**短期限特有**的（本轮新测，不是更正）

H 补格显示：M-T 对同 H 母体的中位在 H=3 是 +0.192/+0.229、H=5 是 +0.232/+0.209、
H=10 降到 +0.206/+0.004、**H=20 变成 −0.089/−0.326**（正比例 44%/17%）。
事件剖面独立印证：加回集 5 日 +19.8 bp vs 移出集 +13.8（差 +6.0），
但 20 日翻号（+22.1 vs +38.2，差 −16.1）。

**影响**：M-T 的结论必须带持有期限定。而 L 块显示加冲击后 A≥5 亿的最优 H 是 15–20 ——
**正是 M-T 增益消失的区间**。这两块的张力要写进 REPORT。
