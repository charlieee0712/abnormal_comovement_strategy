# E6f 源码/事实勘误 (source_corrections_E6f.md)

本轮新发现与已确认的事实更正。brief §0.1 已承认的 proposal 六处错另见 brief 与 `E6f_proposal.md` 顶部勘误。

## 1. `pd.qcut` 的分位边界不能用闭式复现 (本轮新发现, 已修)
把 `pd.qcut(rank('first'), g)` 的组号写成闭式 `gid = searchsorted(1 + k/g*(n-1), r)`, 与 pandas 相比
在 **末位 1 ULP** 上不同: pandas 的边界由 `np.percentile` 的线性插值算出, 恰好落在边界上的那一个秩会翻组。
实测 `drop_mask_dense C:k15` 与源 helper **差 28 格**(k=5/10/20 恰好不差, 所以只测这三个会漏掉)。
**修法**: `qcut_group(n, g)` 直接调用 pandas 本身并按 `(n, g)` 记忆化 —— 构造上就不可能不同, 且记忆化后开销可忽略。
**教训**: 复刻一个库函数的**分箱边界**时, 不要重新推导公式; 要么调用它, 要么对全部会用到的参数逐一比对。
只测了参数空间的一部分 (k∈{5,10,20}) 就宣布等价, 是这次差点漏掉的原因。

## 2. `PATHS['cache_dir']` 指向公共只读目录 (本轮新发现)
`data_loader.PATHS['cache_dir'] = /mnt/big/base/public/FundamentalTL/cache/` 属于另一位同事, **当前用户不可写**。
四段冻结区间的 kline 缓存已在其中, 所以 E6e 从未触发; 但 `history_warmed` 要加载**新日期区间**, 会在写缓存时
抛 `PermissionError`。**修法**: `e6f_core.shadow_cache()` 建一个自己盘上的影子目录, 把公共缓存全部**符号链接**
进去(读命中、零拷贝), 再把 `PATHS['cache_dir']` 指过去; 新区间落到自己盘, 公共目录一个字节都不写。

## 3. 源数据延伸到 2026-09-09, 冻结末日是 2026-03-27 (本轮新发现)
源 kline 目录的最晚日期是 **20260909**, 即 E7 期数据物理上就在盘上、可读。`PERIODS` 的第四段末日是 20260327。
两者之间没有任何技术屏障, 一个 `end_date` 笔误就是禁区 §12 违规 + GLOBAL STOP。
**修法**: 全部取数走 `e6f_core.guarded_load()`, 内部 `end_date > '20260327'` 直接抛 `RuntimeError`;
自测第 3 组把 `20260909` 与 `20260328` 两个反例都跑一遍, 必须被拒。

## 4. E6e `run_manifest.json` 的 SHA 表写于阶段 A, 只覆盖 8 个文件
表里是四地基 + `comprehensive_factor_diagnosis.py` + 当时已存在的 `e6e_core/e6e_run/e6e_stageA`。
`e6e_selftests / e6e_merge1 / e6e_random / e6e_stats` 写在 manifest 之后, **本就不在表里**, 不是"对不上"。
**正确核对方法** = manifest ∩ 现存文件全部 SHA 相同 **且** 七个 `e6e_*.py` 全部 `git status` 干净。
本轮两条都过。**教训**: 拿一个部分清单当全量清单核对, 会造出假的红灯。

## 5. `days_nohold` 全部落在段首预热, 段内没有空仓日 (本轮新事实)
按 `daily_pos` 反推: 全窗口 `days_nopos` 中位 124 天, 其中**段首连续预热 124 天, 段内 0 天**。
`benchmark_missing` 恒为 76 = 4 段 × 19 天(`mature` 20 日过滤)。
即 §1.3 说的"`days_nohold` 每段 19~29 天由 `mature` 与 tvol 的 `min_periods` 造成、不是 bug"得到独立证实:
**没有任何一天是因为选股规则本身选不出票而空仓的**。

## 6. 停牌日 `amount` 恒为 0 而非缺失 (语义确认, 影响 ADV20)
实查: 停牌日 `amount` 非零占比 0.000000、缺失占比 0.000000 —— 即恰好是 0。开市日中位约 1.05e+08 (元)。
故 §6.1 的"停牌日 amount=0 只在语义确认后计入"条件已满足。**本轮主口径取交易日**
`ADV20_trading = mean(amount | is_open==1, e-20..e-1, 至少 10 个有效日)`,
并列给含 0 的 `ADV20_calendar` 作敏感性列 —— 两者差异本身就是停牌频率的度量。

## 7. `industry_zx1` 是 PIT 的
相邻交易日之间行业标签有变化(实查 2024Q1 有 9,769 格变化), 日均约 30 个一级行业, 非空率 98%+。
故 DEV 削顶与行业中性化都用它; 缺失固定成 `__UNKNOWN__` 一类, **不用未来行业回填**。

## 8. `e6e_random.py` 的退出问题不在本轮修 (保 provenance)
E6e 阶段 E 的 89 个进程写完 `_DONE` 与全部 parquet 后卡在解释器退出(parquet writer 的非守护线程)。
本轮**不改该脚本**, 只在 `limit_register.md` 登记; 新写的 E6f 脚本一律走 close/join/flush/fsync + 原子 rename,
父进程验收产物后才写 DONE, 不默认 `os._exit(0)`。

## 9. `drop_duplicates` 取元数据会静默取到"第一段"（E6f 阶段 0 修）
`e6e_merge1.py` 的 `full_row()` 用 `meta = sf.drop_duplicates(subset=['config_id'])` 从**分段**表里取元数据。
`drop_duplicates` 保留首次出现 = **2010-2014 段**。分类字段（kind/core/veto/parent/family/cset/variant）各段
相同、不受影响；但同一句循环里还带了两个**数值**字段 `avg_nh` 与 `days_nohold`，于是全窗口表里这两列
其实是首段值。`KTC_mean@30|cvr_1d:k10+cr5:k10` 的 `avg_nh` 写作 41.9，真值 79.2（各段 42/69/99/139）；
`days_nohold` 写作 29，真值 116。全表 `avg_nh` 新/旧比值中位 **1.87**（最小 1.77，最大 1.94）——
即旧表把持仓票数系统性低估了近一半，且低估幅度随配置而异。

**修法**（`e6f_e6e_summary_fix.py` → `revisions/E6e_summary_fix/`，不覆盖原表）：
`avg_nh` 的每段定义是 `mean(nh[nh>0])`，分母 = 该段有持仓天数 `n_pos_s = T_s − days_nohold_s`，
故全窗口精确值 = `Σ_s(avg_nh_s × n_pos_s) / Σ_s n_pos_s`（**不是**按段总天数加权，那样得 79.5；也不是
等权，那样得 79.3）。`days_nohold_full = Σ_s days_nohold_s`。另补各段分列。
非目标列复算与原表最大差 **7.1e-15**（浮点末位），`n`/`n_seg_pos` 精确相同，前沿集合与 core_depth 行集合
对称差均为 0 —— 即 net8/net12/gross/turn/pos 及一切基于它们的结论**不受影响**，E6e 的采纳判断不变。

**教训**：把分类字段和数值字段写进同一个 `for k in (...)` 元数据循环，是这个 bug 的载体。
分段结果聚合成全窗口时，**每个数值列都要显式写出它的聚合权重**；凡是"取一行代表"的写法，
只允许用于已验证各段恒等的字段。验收侧的对称提醒：汇总表里任何"平均持仓数"级别的量，
应有一个独立锚（如逐段值的范围）能一眼看出全窗口值是否落在段值区间内 —— 41.9 落在 [42,139] 之外
（恰好压在下界），本可当场发现。

> 本条纠的是 **E6e 汇总表**, 但发现与修法都在 E6f 本轮 (阶段 0)。修订产物在
> `revisions/E6e_summary_fix/`, **不覆盖 E6e 原表**; E6e 仓库里的 `source_corrections.md`
> 保持 HEAD 原样 —— brief §11 授权 git 提交的是本文件, 不含改动 E6e 的文档。

## 10. 「买不成留现金」写成了两个账户的现金总量比较 (本轮新发现, 已修)
`e6f_c2.py` 第 10 组原断言: **X0 现金 ≥ Xideal 现金 的天数占多数**。四段实测 4.6% / 32.6% / 63.3% / >50%,
前两段判 FAIL、整个模块走 MODULE LIMIT、不产出账户表。

**这条断言本身不成立**, 有两个独立原因:
1. X0 不只买不成、**也卖不出**。卖单被挡时仓位被困在票上、换不成现金, X0 的现金反而比 Xideal **少**。
   2015-2018 是股灾段(大面积停牌 + 跌停封死), 卖不出压倒买不成 —— 67% 的天数现金更少是模型
   **如实反映市场**, 不是恒等式不成立。2010-2014 的 4.6% 同理(小盘为主、ST 涨跌幅 ±5%)。
2. 两个账户的持仓一旦分叉, 之后每天的现金差就由**各自不同的组合**决定, 与"今天有没有被挡"无关。
   拿一个累积量去测一个当日机制, 本来就不可识别。

**修法**: 换成订单层的真不变量 —— 在 `shadow_account` 里累计 `bad_fill[t] = Σ|du[被挡买单]| + Σ|du[被挡卖单]|`,
断言 **X0 + X1 + X1u 三个账户上恒为 0 股**(被挡的单一股都不许成交)。原来那个比例改为**诊断行**报出,
连同 X0 的未成交卖单名义额一起, 用来描述各段被挡的方向构成。

**教训**: 「买不成留现金」这句话的形式内容是**被挡的买单不产生持仓**, 不是**账户现金更多**。
把一个**单日、单侧**的机制写成**跨账户、累积**的比较, 就会在市场条件改变方向时假报失败 ——
而且失败得最响的恰好是模型最该被信任的那一段(股灾段的卖出受阻)。验收侧的对称提醒:
凡断言里出现"占多数""大部分天数", 先问它是不是恒等式; 不是恒等式的, 应该进诊断行而不是判据。

## 11. 影子账户的基准扣减用错了仓位口径 (本轮新发现, 已修) —— 假象 4.45 点/年
`e6f_c2.shadow_account` 里 `pos_t = (held_at_exec if convention==engine else hold_val)/base`。
`held_at_exec` = 交易后按【执行日 t-1 价】计的持仓 = 第 t 天真正承担的敞口;
`hold_val` = 同一批股数按【估值日 t 价】计 = 当天涨跌**之后**的市值。而当日损益
`r_port = (nav - prev_nav)/base` 恰恰就赚在 `held_at_exec` 上。

超额按 `excess = r_port - BENCH·pos` 算。account 口径下用了 `hold_val`, 而
`hold_val/held_at_exec = 1 + r_p`, 于是
`excess_账户 = excess_引擎 - BENCH·pos·r_p`, 年化后白扣掉 **252·pos·Cov(BENCH, r_p) ≈ 252·pos·σ²**。

**实测**(2024-2026, A=5亿, bp=8): 修前 Xideal - Xengine = **-4.4474 点/年**, 拆开是
价格路径 +0.0080 + 账户约束 -4.4554; 修后 = **+0.0058 点/年** = +0.0078 + (-0.0020)。
四段的"账户模型差"修前为 -2.30 / -6.56 / -2.55 / -4.82, **与各段日波动率排序一致**
(股灾段最大、平静段最小) —— 这正是 σ² 的指纹, 不是任何经济摩擦。

**怎么发现的**: 原代码把这一项当成"账户模型差(仓位随日收益漂移 + 复牌跳变不漏记)"**推理归因**后
直接报出。本轮加了一个只翻价格路径、其余约定不动的开关 (`price_path`), 把该差**加法拆成两项**;
价格路径只占 +0.008, 逼出剩下的 -4.455 必须来自别处, 再顺着 `convention` 的三处分支查到仓位口径。
现金约束被 `self_financing`(scaled_down=0 天) 与 `constant_notional`(6 天) 的对照排除 (只差 0.019)。

**修法**: 基准扣减一律用 `held_at_exec/base`; 盯市仓位另存 `pos_mtm` 只作报出。
修后 Xengine 锚仍为 7.36e-16, 仓位锚 1.71e-14。

**教训**: 两条。① **"报出而非当错误"的差, 必须先拆到不可再拆再报**——把 4.45 点写成
"账户模型差"挂在报告里, 和把它写成 bug 一样糟, 因为读者会当成实施成本。② 凡是
`excess = 组合收益 - 基准 × 仓位` 这类式子, **仓位必须与收益是同一时点的敞口**;
用收盘盯市去配当日收益, 等于把当天自己的涨跌乘进基准扣减, 误差量级恰是 252σ² —— 在
A 股日波动下就是 4~7 点/年, 大到足以淹没本轮全部实施摩擦 (成交约束实测只有 ±0.13)。
