# E6e source_corrections —— 本轮对既往说法的收窄与更正

用途：记录 E6e 执行过程中发现的、对既往文档（proposal / web plan / brief / 既往 REPORT）说法的更正。
只记事实层更正，不含结论。

## 1. proposal §1.2 的 DEV 仓位机制写反（brief §0.1 已采纳 web 的更正，此处存档）
`assign_weights_dev` 是 `w = np.full(n, min(1/n, 0.01))` 再按行业 `cap = share + 0.03` 等比缩减。
故 **n < 100 时 1% 上限绑定**（总仓位 `P(n) = 0.01n`），剔票**降仓位**；**n > 100 时 base = 1/n**，剔票不降仓位。
含行业削顶时剔票**甚至可能升仓位**——玩具测试 G1 实测：n=150 剔 10 只，总仓位 0.8467 → 0.9000。

## 2. brief §2.3 的「19 种 × P114 = 2,280」是笔误
正确是 19 × 114 = **2,166**。§3.7 的「3,570 条规则 = P114 × 19 硬 + P54 × 26」= 114×19 + 54×26 = 2,166 + 1,404 = 3,570 才自洽。
本轮按 2,166 执行；`summary/veto_marginals.csv` 实得 3,570 行（2,166 硬 + 972 复合 + 432 豁免）与之吻合。

## 3. brief §5 阶段 C 的「等仓位（P54 系）」与 §0.1 冲突
§0.1 明写「采纳，全部子配置都做（硬、复合、豁免、软覆盖层）；不按核族缩范围（初稿曾限 P54 系，已撤回）」。
§5 的括注是初稿残留。本轮按 §0.1 执行**全覆盖**：每段 7,140 个等仓位配置（3,570 对 × 父/子各一）。

## 4. 引擎契约补充：小 universe 不做中性化
引擎导入的是 `pool_screening_v2` 里的 `neutralize_by_mcap`（不是 `factor_ic_diagnosis` 那份，后者另有 `var_x` 保护）。
该函数在**当日有效票不足 10 只时直接返回原始因子值、不做中性化**；而 `precompute_neutralized_factor` 的入池门槛是 6 只。
即 universe 为 6–9 只的日子用的是**未中性化的原始因子**。E6d 已记，本轮复核仍成立，已写进 `engine_contract.md`。

## 5. `pool0` 的列宽度不是"小池"
I11 是事件观察池：每天约 120–480 只，但**一段之内出现过的票有 2,499 ~ 5,034 只**（几乎全市场，每只票平均触发约 10 次）。
任何"按 pool0 成员做稠密矩阵"的实现都会有约 97% 的空转。E6e 的稀疏内核据此设计。

## 6. 随机键的实现偏离（经用户 2026-09-06 确认）
brief §3.7 字面写 `SHA256("E6e-v1", profile, seed, date, ticker)` 逐元素派生。逐元素需 1.52 亿次 hashlib/段（30–50 分钟纯开销）。
实际实现：`SHA256(VERSION|profile|seed|date)` → u64 密钥，再对**冻结票码表**做向量化 64 位混合器。
性质等价（对 ticker 重排等变、与分片/并发顺序无关、换 seed·profile·日期即变、均匀），验收用玩具测试第 6 组，已过。
